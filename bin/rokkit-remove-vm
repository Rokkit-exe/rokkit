#!/bin/bash

vm_name=$1

[ ! -z $vm_name ] && echo "Usage: $0 <vm_name>   to shudown, destroy and undefine the vm"

# Stop the VM if it's running
virsh shutdown rokkit

# Or force it off
virsh destroy rokkit

# Remove the VM definition
virsh undefine rokkit
